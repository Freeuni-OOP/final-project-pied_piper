# LecturBoxd Frontend Environment Configuration
# Backend API base URL - adjust the port if your backend runs on a different port
VITE_API_BASE_URL=http://localhost:8081

# WebSocket URL for real-time chat
VITE_WS_URL=ws://localhost:8081/ws

# App environment
VITE_APP_ENV=development

